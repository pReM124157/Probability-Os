import { describe, it } from "vitest";

describe("legacy outcome lifecycle wrapper", () => {
  it.skip("canonical outcome lifecycle coverage lives in src/tests/outcome-lifecycle.test.js", () => {});
});
